import { Box, Input, Text } from "@chakra-ui/react";

const Search = () => {
  return (
    <Box>
      <Input placeholder="Search for your destination..." maxWidth="100%" />
    </Box>
  );
};

export default Search;
